import type { EntityFamilyConfig } from "@/types/entities";

// No first-launch entity families per Site Plan: recipes, romance NPCs,
// achievements, and DLC are each covered by dedicated fixed pages.
export const entityFamilies: EntityFamilyConfig[] = [];